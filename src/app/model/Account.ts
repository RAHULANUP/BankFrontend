export interface Account {
    accountNumber: number;
    customerId: number;
    customerName: string;
    balance: number;
}

export interface AccountDto {
    accountNumber: number;
    customerId: number;
    customerName: string;
    balance: number;
}
